rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: v1 scaffold rules (permissive). Tighten before broad use.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
